# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS / 编辑器
.DS_Store
.idea/
.vscode/

# 环境（common 自己不存配置；各仓配置在各仓 <仓>/.env）
.env
.env.*

# 组合部署：pull-code 把各仓拉到 deploy/repos/（含各仓 .env），构建暂存到 deploy/.stage/——都不入库
deploy/repos/
deploy/.stage/
